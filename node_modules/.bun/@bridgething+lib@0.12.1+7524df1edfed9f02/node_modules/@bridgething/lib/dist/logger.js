const LogVerbosity = {
    Trace: 5,
    Debug: 4,
    Log: 3,
    Warn: 2,
    Error: 1,
    Silent: 0,
};
class Logger {
    name;
    logLevel;
    constructor(name, logLevel) {
        this.name = name;
        this.logLevel = logLevel;
    }
    trace = (...data) => this.logLevel >= LogVerbosity.Trace ? console.log(`${this.name} TRACE >>`, ...data) : null;
    debug = (...data) => this.logLevel >= LogVerbosity.Debug ? console.log(`${this.name} DEBUG >>`, ...data) : null;
    log = (...data) => this.logLevel >= LogVerbosity.Log ? console.log(`${this.name} LOG   >>`, ...data) : null;
    warn = (...data) => this.logLevel >= LogVerbosity.Warn ? console.warn(`${this.name} WARN  >>`, ...data) : null;
    error = (...data) => this.logLevel >= LogVerbosity.Error ? console.error(`${this.name} ERROR >>`, ...data) : null;
}
export { LogVerbosity, Logger };
//# sourceMappingURL=logger.js.map