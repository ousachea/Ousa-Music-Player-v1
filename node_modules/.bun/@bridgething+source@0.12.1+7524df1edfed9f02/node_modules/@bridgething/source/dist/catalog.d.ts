import { type Catalog } from '@bridgething/catalog';
import { type App, type Ledger, type SourceConfig } from './lib.ts';
export interface StageResult {
    catalog: Catalog;
    ledger: Ledger;
    added: {
        slug: string;
        version: string;
    }[];
}
export declare function readLedger(site: string): Promise<Ledger>;
export declare function stage(site: string, apps: App[], ledger: Ledger, base: string): Promise<StageResult>;
export declare function generate(source: SourceConfig, apps: App[], ledger: Ledger, base: string, site: string): Promise<Catalog>;
export declare function validate(catalog: unknown): Catalog;
//# sourceMappingURL=catalog.d.ts.map